cmake_minimum_required(VERSION 3.19)
project(qscintilla LANGUAGES CXX)

# 继承项目全局编译配置
set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
set(CMAKE_CXX_EXTENSIONS ON)

# 查找Qt依赖
find_package(Qt5 REQUIRED COMPONENTS Core Gui Widgets PrintSupport)

# 自动收集所有源文件和头文件
file(GLOB QSCI_SOURCES ${CMAKE_CURRENT_SOURCE_DIR}/Qt4Qt5/*.cpp)
file(GLOB QSCI_HEADERS ${CMAKE_CURRENT_SOURCE_DIR}/Qt4Qt5/Qsci/*.h)
file(GLOB SCINTILLA_SOURCES ${CMAKE_CURRENT_SOURCE_DIR}/src/*.cpp)
file(GLOB SCINTILLA_HEADERS ${CMAKE_CURRENT_SOURCE_DIR}/include/*.h ${CMAKE_CURRENT_SOURCE_DIR}/src/*.h)
file(GLOB LEXERS_SOURCES ${CMAKE_CURRENT_SOURCE_DIR}/lexers/*.cpp)
file(GLOB LEXLIB_SOURCES ${CMAKE_CURRENT_SOURCE_DIR}/lexlib/*.cpp)

# 静态编译标识
add_definitions(-DQSCI_STATIC -DSCINTILLA_QT)

# 包含目录配置
include_directories(
    ${CMAKE_CURRENT_SOURCE_DIR}/Qt4Qt5
    ${CMAKE_CURRENT_SOURCE_DIR}/include
    ${CMAKE_CURRENT_SOURCE_DIR}/src
    ${CMAKE_CURRENT_SOURCE_DIR}/lexlib
)

# 生成静态库
add_library(qsci STATIC
    ${QSCI_SOURCES}
    ${QSCI_HEADERS}
    ${SCINTILLA_SOURCES}
    ${SCINTILLA_HEADERS}
    ${LEXERS_SOURCES}
    ${LEXLIB_SOURCES}
)

# 明确指定链接语言为C++，启用AUTOMOC处理Qt信号槽
set_target_properties(qsci PROPERTIES LINKER_LANGUAGE CXX AUTOMOC ON)

# 链接Qt库
target_link_libraries(qsci PRIVATE
    Qt5::Core
    Qt5::Gui
    Qt5::Widgets
    Qt5::PrintSupport
)

# MSVC编译选项适配
if(MSVC)
    target_compile_options(qsci PRIVATE /utf-8)
    target_compile_definitions(qsci PRIVATE
        UNICODE
        _UNICODE
        _CRT_SECURE_NO_WARNINGS
    )
endif()

# 导出头文件目录给上层项目使用
target_include_directories(qsci INTERFACE
    $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/Qt4Qt5>
    $<INSTALL_INTERFACE:include>
)
