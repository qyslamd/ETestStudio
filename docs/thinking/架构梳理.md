# 设计讨论：架构梳理

一次关于整体架构的讨论记录，梳理了当前对分层、数据流、各层职责的思考。**不代表最终设计**，后续可能根据实际情况调整。

---

## 一、五层架构（当前思路）

```
┌─────────────────────────────────────────────────┐
│                   应用层 (UI)                     │
│  设备管理器 | ICD编辑器 | 用例编辑器 | 监控面板     │
├─────────────────────────────────────────────────┤
│                 测试引擎层 (Engine)                │
│  用例调度 | Lua虚拟机 | 断言/验证 | 报告生成       │
│  API: SetDevice / VerifyDevice / InjectFault     │
├─────────────────────────────────────────────────┤
│               ICD信号层 (ICD)                     │
│  信号路由(工程值↔通道映射) | 协议转换(pack/unpack) │
│  故障注入(FaultManager) | SignalValue转换         │
├─────────────────────────────────────────────────┤
│                   数据池 (DataPool)                │
│  原始字节通道(Pub/Sub): 原始字节 + 元数据         │
│  SignalValue通道(Pub/Sub): 工程值 + 时间戳 + 质量  │
├─────────────────────────────────────────────────┤
│              硬件抽象层 (HAL)                     │
│  设备插件(按类型特化): AD/DA/CAN/1553B/A429/串口  │
│  虚拟设备(Simulate模式) | 厂商SDK封装             │
└─────────────────────────────────────────────────┘
```

### 层间依赖思路

- 上层调用下层接口，下层不反向依赖上层
- 层间通过接口通信
- ICD 和 Engine 不感知数据池——ICD 读写数据池通过内部封装，Engine 不碰数据池

---

## 二、数据流

### 写路径（Engine → 硬件）

```
Lua: SetDevice("温度", 37.5)
         │
         ▼
Engine --ICD.Set("温度", 37.5)──▶ ICD(查映射→查故障注入→协议封包→SignalValue)
                                       │
                                       ├──▶ 数据池.原始字节通道.Publish(原始字节 + 元数据)
                                       │        │
                                       │        ▼
                                       │    HAL.Subscribe() → 发硬件
                                       │
                                       └──▶ 数据池.SignalValue通道.Publish(工程值+时间戳+质量)
                                                │
                                                ▼
                                            UI 监控订阅
```

### 读路径（硬件 → Engine）

```
HAL(收硬件) --Pub──▶ 数据池.原始字节通道(原始字节 + 元数据)
                          │
                          ▼
                    ICD.Subscribe() → 解协议 → SignalValue
                          │
                          ├──▶ 数据池.SignalValue通道.Publish(工程值+时间戳+质量)
                          │        │
                          │        ├──▶ UI 监控订阅
                          │        └──▶ Engine.Get() 快速读取最新值
                          │
                          └──▶ ICD内部CVT缓存(供下次Get直接返回)
```

### 信号数据结构（草案）

**原始字节元数据**
```cpp
struct RawData {
    QString deviceId;       // 设备唯一ID
    QString channelId;      // 通道ID
    QByteArray data;        // 原始字节
    qint64 timestamp;       // 采集/收发时间(微秒)
    QString protocolId;     // 关联协议ID(ICD层填入)
};
```

**SignalValue**
```cpp
struct SignalValue {
    QString signalId;       // 信号唯一ID
    QVariant value;         // 工程值
    qint64 timestamp;       // 微秒级时间戳
    bool isValid;           // 有效性
    int quality;            // 数据质量(0=正常, 1=可疑, 2=无效, 3=模拟)
    QString sourceInfo;     // 来源描述(设备/通道/协议等)
};
```

---

## 三、Dry Run（影子运行）

### 思路

```
Engine ──ICD API──▶ ICD ──▶ 数据池 ──▶ HAL(Simulate模式: 虚拟设备生成数据)
                       │                      │
                       ◀──────────────────────┘
```

- Engine 和 ICD 在 Dry Run 模式下无感知
- HAL 层每个插件提供 `simulate()` 模式，切到虚拟数据源
- 数据池不变，数据来源从真实硬件换成虚拟设备
- UI 监控面板无感知，正常订阅 SignalValue 通道

### 可能的触发方式

- 全局开关：整个系统进入 Dry Run，所有设备走虚拟
- 单设备开关：部分设备真实、部分设备虚拟（混合模式）
- 开关位置：HAL 层插件级别，不向上透传

---

## 四、故障注入

### 在用例层面控制（初步想法）

```
Engine --ICD.InjectFault(signalId, FaultConfig)──▶ ICD内部FaultManager
                                                       │
                                                       ▼
                                               影响ICD转换过程:
                                               - 信号值超限/死滞/固定值
                                               - CRC/校验错误
                                               - 协议字段篡改
                                               - 延迟/丢包(委托数据池)
```

### 故障类型

| 类型 | 实现位置 | 说明 |
|------|---------|------|
| 信号值死滞(Stuck-at) | ICD 转换 | 温度固定读 999 |
| 信号值加偏置/噪声 | ICD 转换 | AD 值多加 5% |
| 协议 CRC 错误 | ICD 封包 | CAN 帧 CRC 字段写反 |
| 校验位错误 | ICD 封包 | 429 label 校验位取反 |
| 通信延迟 | 数据池 | 数据延迟 N ms 到达 |
| 通信丢包 | 数据池 | 按概率丢弃原始字节 |
| 采样率异常 | 数据池 | 数据频率突然变化 |

### 清除

- `ICD.ClearFault(signalId)` — 清除单信号故障
- `ICD.ClearAllFaults()` — 清除所有故障
- 用例结束时自动清除所有活跃故障（待确认）

---

## 五、ICD 层职责

### 双重角色（讨论中形成的看法）

1. **信号路由（Signal Routing）**
   - 管理"信号名 → 设备ID + 通道ID + 协议ID"的映射关系
   - 管理"信号名 → 转换规则"的映射（线性/多项式/枚举/脚本）
   - SignalValue 总线发布/订阅（供 UI 消费）

2. **协议转换（Protocol Conversion）**
   - pack：工程值 → 原始字节（按协议定义封包）
   - unpack：原始字节 → 工程值（按协议定义解包）
   - 字节序处理（每字段独立配置大端/小端）
   - 协议校验（CRC/和校验/异或）

### ICD 内部结构（草案）

```
ICD Manager
├── SignalMapper          // 信号路由: signalId → deviceId + channelId + protocolId
│   └── 转换规则(线性/多项式/枚举/脚本)
├── ProtocolRegistry      // 协议定义仓库
│   └── Protocol{字段列表 + 字节序 + 校验 + pack/unpack}
├── FaultManager          // 故障注入管理
│   └── 活跃故障列表 {signalId, type, params, active}
├── SignalValueCache      // CVT: 信号最新值缓存(供Engine.Get快速读取)
└── DataPoolAdapter       // 数据池读写封装(上层无感知)
```

---

## 六、HAL 插件设计原则

### 按类型特化，不追求万能接口（个人倾向）

```
IDevicePlugin(基类: 生命周期+元数据)
├── ISerialPlugin          // 串口族: RS232/422/485
│   ├── BaudRate/DataBits/Parity/StopBits 配置
│   └── asyncWrite/dataReceived
├── ICanPlugin             // CAN/CAN FD
│   ├── BitRate/IDFilter/Mode 配置
│   ├── sendFrame(标准帧/扩展帧)
│   └── frameReceived
├── IARINC429Plugin        // A429
│   ├── Label/SDI/SSM/BitRate 配置
│   ├── sendLabel()
│   └── labelReceived
├── IMil1553Plugin         // 1553B: BC/RT/MT
│   ├── Mode 配置(BC/RT/MT)
│   ├── bcSchedule/Msg
│   └── rtResponse/mtMonitor
├── IADevicePlugin         // 模拟量采集
│   ├── Channels/SampleRate/Range/Trigger 配置
│   ├── start采集/stop采集
│   └── dataReady(多通道数据)
├── IDADevicePlugin        // 模拟量输出
│   ├── Channels/Range 配置
│   └── setOutput(channel, value)
├── IDioPlugin             // 开关量IO
│   ├── 通道方向配置(输入/输出)
│   ├── readInput(channel)
│   └── setOutput(channel, value)
├── ITcpPlugin             // TCP(走标准Qt)
│   ├── 模式(client/server)
│   ├── asyncWrite/dataReceived
│   └── clientConnected/disconnected
```

### 所有插件额外能力

- `simulate(bool enable)` — 切换到虚拟数据源
- `selfTest()` — 自检
- `deviceStatus()` — 当前状态(在线/离线/异常/模拟)
- `configMetaData()` — 配置参数元数据（可用于自动生成配置界面）

---

## 七、Engine 层设计思路

### 与 ICD 的交互方式

Engine **不直接操作数据池**，所有硬件/信号操作通过 ICD API（待定型）：

```cpp
class IICDEngineInterface {
    virtual bool setSignal(const QString &signalId, const QVariant &value, QString *err = nullptr) = 0;
    virtual SignalValue getSignal(const QString &signalId, bool *ok = nullptr) = 0;
    virtual bool verifySignal(const QString &signalId, const QVariant &expected,
                              const Tolerance &tolerance, QString *err = nullptr) = 0;
    virtual bool injectFault(const QString &signalId, const FaultConfig &fault, QString *err = nullptr) = 0;
    virtual void clearFault(const QString &signalId) = 0;
    virtual void clearAllFaults() = 0;
};
```

### Lua API 风格（初步设想）

```lua
SetDevice("温度", 37.5)
VerifyDevice("温度", 37.5, {min=-0.1, max=0.1})
InjectFault("温度", {type="stuck_at", value=999})
Delay(1000)
```

---

## 八、与其他文档的关系

| 文档 | 关系 | 说明 |
|------|------|------|
| docs/thinking/构思.md | 早期需求 | 本讨论的部分想法源于此，但不代表继承关系 |
| docs/thinking/初步计划.md | 早期设计 | 四层架构思路，本讨论演变为五层 |
| docs/thinking/技术方案汇报.md | 早期演示 | 对外文稿，与本次讨论无关 |
| docs/thinking/IATP_Core_Technical_Points.md | 早期技术笔记 | 部分概念（故障注入、Dry Run）在本讨论中延续 |
| docs/thinking/IATP_Comprehensive_Design_Spec.md | 早期技术笔记 | 同前 |
| docs/01-规划/ | 阶段性计划 | 各阶段计划基于当时的理解编写，与本讨论可能有出入 |
| docs/03-开发/ | 开发记录 | 阶段1的开发实现记录，反映当时的实际进度 |
| docs/02-研究/ETest_Help_Docs/ | 参考资料 | 竞品研究，与本讨论方向无关 |
| docs/04-参考/ | 参考资料 | Lua、sol2等技术参考，保留即可 |

---

## 九、遗漏模块与待补充设计

以下模块在早期文档（`构思.md`、`初步计划.md`、`整体架构设计.md`）中均有提及，但在本次架构梳理中被遗漏。**后续设计阶段需补充**。

### 9.1 帧协议编辑器（Frame Protocol Editor）

**来源**：`docs/thinking/构思.md`
**为什么漏了**：ICD 层虽然定义了"协议转换（pack/unpack）"，但协议的**定义来源**没有交代。

**需要回答的问题**：
```
"谁创建了协议定义？" → 帧协议编辑器
```

用户通过帧编辑器以可视化方式定义：
- 帧的字段布局（起始比特位、字段长度）
- 字节序（每字段独立配置大端/小端）
- 编码方式（无符号整数、有符号整数、浮点数、BCD 码等）
- 协议校验（CRC / 和校验 / 异或）

帧编辑器产出的协议模板 → `ProtocolRegistry` → ICD 的 pack/unpack 使用。

**后续确认项**：
- 与 ICD 编辑器的边界：帧编辑器定义"比特流结构"，ICD 编辑器定义"工程值映射"
- 是否需要反向同步：用户在帧编辑器改了一个位域定义，关联的 ICD 信号应自动感知

### 9.2 硬件连接拓扑编辑器

**来源**：`docs/thinking/构思.md`
**为什么漏了**：架构梳理没有体现设备之间的连接关系管理。

用户通过拓扑编辑器描述：
- 硬件设备与被测设备（UUT）之间的物理连接关系
- 哪个设备的哪个通道接到了哪个设备的哪个通道
- 连接线的类型（信号线、总线、网络等）

**作用**：
- ICD 映射的前置条件：信号→设备的哪个通道→通过哪条连接线
- 支持拖拽连线方式编辑（Qt GraphicsView 框架实现）

**后续确认项**：
- 与 ICD 信号映射的关系：连接关系是否作为 ICD 映射的输入条件
- 是否支持多级嵌套（设备→板卡→通道）

### 9.3 多脚本语言支持

**来源**：`docs/thinking/初步计划.md`、`docs/thinking/构思.md`
**现状**：架构梳理只展示了 Lua API 风格。

**待确认**：
```
Engine 支持几种脚本语言？统一抽象怎么做？
```

| 脚本语言 | 嵌入方式 | 适用场景 |
|----------|---------|---------|
| Lua | sol2（已集成） | 轻量级测试脚本，资源受限场景 |
| Python | pybind11 / CPython 嵌入 | 复杂计算、数据分析、生态库调用 |
| JavaScript | QJSEngine / QuickJS | Web 前端一致性、非专业用户 |

三种脚本语言通过统一的 `IScriptEngine` 抽象接口管理，Engine 不直接绑定具体语言：

```cpp
class IScriptEngine {
    virtual bool evaluate(const QString& script) = 0;
    virtual void setGlobalVariable(const QString& name, const QVariant& value) = 0;
    virtual QVariant callFunction(const QString& name, const QVariantList& args) = 0;
    virtual void setICDInterface(IICDEngineInterface* icd) = 0;
    // ... 断点/单步/变量监视等调试接口
};
```

**后续确认项**：
- 是否所有测试用例都统一用一种脚本语言？（初期可固定为 Lua，降低复杂度）
- 脚本引擎的断点/单步调试接口是否需要统一抽象

### 9.4 测试执行控制（断点/单步/暂停恢复/并行）

**来源**：`docs/thinking/初步计划.md`、`docs/thinking/构思.md`
**现状**：架构梳理的 Engine 只展示了顺序执行。

**缺失的执行控制特性**：

| 特性 | 说明 |
|------|------|
| 暂停/恢复 | 用户点击暂停→当前步骤执行完后挂起；点击恢复继续 |
| 终止 | 立即停止执行，标记用例为"已终止" |
| 断点 | 在特定步骤或信号条件满足时暂停执行 |
| 单步执行 | 每次执行一个步骤后暂停 |
| 并行执行 | 多个测试用例同时执行，或一个用例中的多个步骤并行 |
| 超时处理 | 每个步骤可配置超时时间，超时后触发异常处理 |

**后续确认项**：
- MVP 阶段是否需要并行执行？（建议：顺序执行先实现，并行后续补充）
- 断点是用例级还是步骤级

### 9.5 数据记录与回放

**来源**：`docs/thinking/整体架构设计.md`、`docs/thinking/构思.md`
**现状**：架构梳理无数据持久化模块。

**需要记录的数据**：
- 原始采集数据（AD 采集的电压值、CAN 报文、429 label 等）
- 工程值变化历史（SignalValue 的时间序列）
- 测试执行日志（步骤执行记录、断言结果、故障注入记录）
- 系统状态快照（设备状态、连接状态、ICD 映射状态）

**回放需求**：
- 离线回放：测试执行完成后，加载历史数据重现测试过程
- 在线回放：在监控面板中回放选定时间段的历史波形

**存储选型待定**：
- SQLite：结构化数据（用例、配置、元数据）
- 二进制文件 / HDF5：大规模时序数据（波形、原始报文）
- 时序数据库（InfluxDB / TDengine）：信号值历史（后续选型）

### 9.6 报告生成系统

**来源**：`docs/thinking/初步计划.md`、`docs/thinking/整体架构设计.md`
**现状**：架构梳理报告生成仅一句话带过。

**需要支持的报告格式**：

| 格式 | 方法 | 说明 |
|------|------|------|
| HTML | Qt 模板 + QWebEngine | 丰富样式，嵌入图表 |
| PDF | Html→wkhtmltopdf / libharu（已集成） | 正式文档 |
| Excel | QXlsx（已集成） | 数据表格 |
| CSV | 文本输出 | 轻量数据交换 |

**报告内容**：
- 测试概要（用例名称、执行时间、通过/失败统计）
- 步骤明细（每步的操作、期望值、实际值、判定结果）
- 异常记录（未通过的步骤、超时、设备异常）
- 附图（波形截图、数据曲线）

### 9.7 插件进程隔离

**来源**：`docs/thinking/构思.md`
**现状**：架构梳理未讨论。

**问题**：硬件插件（尤其是通过厂商 SDK 操作的板卡）可能在插件内部 crash 或死锁，如果插件运行在 Engine 进程内，会拖垮整个系统。

**两种方案待讨论**：

| 方案 | 优点 | 缺点 |
|------|------|------|
| **方案 A：同进程隔离**（QPluginLoader） | 简单、延迟低、跨平台 | 插件 crash → 主进程 crash |
| **方案 B：独立进程隔离**（QLocalSocket / QProcess 通信） | 插件 crash → 不影响主进程、可单独重启 | 通信开销大、IPC 编程复杂 |

**折中方案**（当前倾向）：
- MVP 阶段用方案 A（速度快、开发成本低）
- 后续根据稳定性需求评估是否改为方案 B
- 接口层面保持抽象，不暴露 IPC 细节

---

## 十、设计争议点

### 10.1 DataPool 作为独立层的必要性

**当前设计**：DataPool 作为 HAL 与 ICD 之间的独立层。

```
HAL → DataPool → ICD → Engine
```

**争议**：DataPool 是否应该作为独立层，还是作为 ICD 内部组件？

#### 论点 A：DataPool 应作为独立层（当前方案）

- 明确的数据边界：HAL 只管收/发原始字节，不感知工程值
- 支持多个订阅者：UI 监控面板直接从 DataPool 订阅，不经过 ICD
- 便于故障注入：延迟/丢包/采样率异常等故障可在 DataPool 层注入
- 理论上支持"实时数据库"角色，便于后续对接时序数据库

#### 论点 B：DataPool 应作为 ICD 内部组件

- **延迟问题**：数据每次穿越 5 层，对高速采集场景可能成为瓶颈。
  例：EPH5024C 采样率 2.5MSPS → 每秒 250 万次穿越 DataPool 层。
- **必要性不足**：`构思.md` 的说法是"HAL 层读取到的数据考虑引入数据池，永远保存最新值"——
  这实际上就是 CVT（Continuous Value Table），而 ICD 内部已经有 `SignalValueCache`。
- **参考实现**：`AD插件接口设计.md` 中的 Mock AD 插件直接在插件内部维护 `latest_data_` 缓存，
  通过 `dataReady` 信号通知上层，不需要 DataPool 层。
- **复杂度增加**：多一层意味着多一层的调试、配置和错误处理成本。

#### 结论（待定）

```
建议：MVP 阶段将 DataPool 作为 ICD 内部组件（而非独立层）。
理由：
1. MV 阶段的信号量级 < 100，不需要专门的数据池
2. 去掉一层可降低开发复杂度和数据路径延迟
3. ICD 内部的 SignalValueCache 已能覆盖"最新值缓存"需求
4. 后续如需独立数据池（信号量 > 10000、时序数据库对接），可从 ICD 中拆分

数据路径简化后：

  HAL → ICD (含 DataPool 功能) → Engine
                    │
                    └── UI 监控订阅（通过 SignalValue 总线）
```
