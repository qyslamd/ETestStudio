# 常用命令记录

## 删除 Windows 保留设备名文件

```bat
del \\.\D:\path\to\file\nul
```

**说明**: 当误将 `nul` 作为普通文件名创建后（如通过 Git Bash / MSYS 环境执行 copy 命令时），
由于 `nul` 是 Windows 保留设备名，普通 `del nul` 会被解释为清空空设备。
使用 `\\.\` 前缀可以绕过此限制，将其当作普通文件删除。

## TODO: 后续更多命令...

