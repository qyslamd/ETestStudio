# 阶段1.1：开发环境与基础工程搭建迭代（更新版）

## 一、核心目标

完成开发环境统一规范、现有工程CMake配置优化、通过CMake FetchContent集成所有第三方依赖，输出可在VS2017/2019/QtCreator/VSCode一键编译运行的最小Qt程序，所有依赖链接正常，为后续功能开发扫清环境障碍。

## 二、预期周期：1~2个工作日

## 三、开发环境统一规范

| 平台 | 标准配置 | 兼容配置 |
|------|----------|----------|
| Windows | Qt 5.12.12 + VS2017 64位套件 | VS2019 64位套件 |
| Linux | 暂不考虑，后续迭代适配 | - |

* 输出交付物：《Windows开发环境配置手册》，包含Qt、VS安装配置步骤、环境变量统一配置规范

## 四、详细任务拆分

| 序号 | 任务内容 | 输出物 |
|------|----------|--------|
| 1 | 目录结构补充 | 创建顶层`3rdparty/`目录作为FetchContent下载缓存目录，不需要手动存放依赖源码 |
| 2 | 顶层CMakeLists.txt修改 | 在现有基础Qt CMake配置基础上，添加FetchContent配置，拉取所有指定版本的第三方依赖，配置依赖链接规则，编译产物自动输出到`bin/`目录 |
| 3 | 依赖链接验证 | 开发最小验证窗口程序，代码中引入所有依赖的头文件，调用简单API验证链接正常，运行无崩溃、无依赖缺失错误 |
| 4 | 多IDE编译验证 | 分别在VS2017、VS2019、QtCreator、VSCode中执行CMake生成、编译、运行流程，确保所有环境编译正常，无链接错误 |

## 五、工程目录结构（与现有结构对齐）

```
etest_demo/
├── CMakeLists.txt          # 顶层CMake配置，已存在，添加FetchContent配置
├── src/                    # 源代码目录，已存在
│   ├── app/                # 主程序入口，已存在，内置resources资源目录
│   └── CMakeLists.txt      # 已存在
├── 3rdparty/               # 新增，FetchContent依赖下载缓存目录
├── docs/                   # 文档目录，已存在
├── test/                   # 单元测试目录，已存在
├── build/                  # 编译临时目录，已存在
└── bin/                    # 编译输出目录，自动生成
```

## 六、第三方依赖清单（FetchContent拉取指定版本）

| 依赖库 | 版本 | 用途 | 协议 | 拉取地址 |
|--------|------|------|------|----------|
| Qt Advanced Docking System | 3.8.3 | VSCode风格停靠布局 | LGPL | github.com/githubuser0xFFFF/Qt-Advanced-Docking-System |
| QScintilla | 2.11.6 | 高级文本编辑器 | LGPL | riverbankcomputing.com/software/qscintilla |
| spdlog | 1.12.0 | 高性能日志 | MIT | github.com/gabime/spdlog |
| gtest/gmock | 1.12.1 | 单元测试框架 | BSD3 | github.com/google/googletest |
| Lua | 5.4.4 | 脚本引擎 | MIT | <www.lua.org/ftp> |
| Qt Xlsx | 0.3.0 | Excel读写 | MIT | github.com/dbzhang800/QtXlsxWriter |
| libharu | 2.4.3 | PDF报告生成，轻量无额外依赖，完美支持Windows7 | MIT | github.com/libharu/libharu |

## 七、完成标准

工程在VS2017/2019/QtCreator/VSCode中均可正常执行CMake生成（自动拉取依赖）、编译、运行，最小窗口程序正常显示，所有依赖链接无错误，无需开发人员额外配置依赖。
