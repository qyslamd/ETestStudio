## 仿真环境里有多个设备的时候，要怎么连线？

多个设备进行连线时，使用总线进行连接。

## 通信协议校验内置函数CheckCode，在取一个字节校验时函数名SUM_8和SUM_8_O8有区别吗？

使用SUM_8,设置字段长度是8位时，结果与使用SUM_8_O8的结果一致，但是要做低8位校验和时，要使用SUM8_O8，如果使用SUM_8，自动验证时会验证失败。如下图所示。

  ![protocol-1](./images/prot-8253.png)
