## 设备自检界面空白显示

原因是执行器连接状态是未连接，server服务器没有起来，不能与执行器进行通信导致。需要重启连接成功后再使用该功能。

## 串口名称设置界面空白显示

原因是执行器连接状态是未连接，server服务器没有起来，不能与执行器进行通信导致。需要重启连接成功后再使用该功能。

## 历史版本-历史回看-查看记录，里面没有内容。

原因是执行配置界面里的记录协议解析、记录通道读写、记录网络变量修改，没有勾选上。勾选后再执行才会有历史回看记录。

